/**
 * @amlplugins/twilio-verify
 *
 * Thin re-export of twilio (CommonJS `export =` SDK).
 *
 * Twilio Verify — 2FA via SMS, voice, email, TOTP, Push, Silent Network Auth.
 */

import _sdk from "twilio";
export { _sdk as sdk };
export default _sdk;
